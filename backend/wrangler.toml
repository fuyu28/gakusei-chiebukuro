name = "gakusei-chiebukuro-api"
main = "src/worker.ts"
compatibility_date = "2024-11-09"
compatibility_flags = ["nodejs_compat"]

[vars]
# 本番の環境変数はCloudflareダッシュボードまたは `wrangler secret put` で設定してください。
